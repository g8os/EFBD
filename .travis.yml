sudo: required
dist: trusty
language: go

jobs:
  include:
    - stage: test
      go: 1.8
      install: skip
      script: TIMEOUT=10m make testgo
    - stage: test
      go: 1.8
      install:
        - ./tlog/install_dependencies.sh
      script: TIMEOUT=10m make testcgo
    - stage: test
      go: 1.8
      install:
        - go get -u github.com/Jumpscale/go-raml
        - ./tlog/install_capnp_0.6.0.sh
      script: make testcodegen
