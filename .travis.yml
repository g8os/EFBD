sudo: required
dist: trusty
language: go

jobs:
  include:
    - stage: test
      go: 1.8
      install: skip
      script: go test -v -timeout 5m github.com/zero-os/0-Disk/zeroctl/cmd/restore  -run ^TestEndToEndReplayBootVdisk$
